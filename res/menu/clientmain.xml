<menu xmlns:android="http://schemas.android.com/apk/res/android" 
    xmlns:yourapp="http://schemas.android.com/apk/res-auto">
    <item android:id="@+id/lastfm"
          android:icon="@drawable/lastfmlogono_small"
          android:title="@string/lastfm"
          android:showAsAction="always"/>
    <item android:id="@+id/off"
          android:icon="@drawable/off"
          android:title="@string/off"
          android:showAsAction="always"/>
    <item android:id="@+id/clientmode"
          android:icon="@drawable/clientmode"
          android:title="@string/clientmode"
          android:showAsAction="never"/>
</menu>
